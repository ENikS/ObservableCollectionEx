version: 1.4.{build}
image: Visual Studio 2017
configuration: Release
platform: Any CPU
before_build:
- cmd: dotnet restore
build:
  parallel: true
  verbosity: minimal
after_build:
- cmd: >-
    cd ObservableCollectionEx\bin\Release
    ren ObservableCollectionEx.0.0.0.nupkg "ObservableCollectionEx.%APPVEYOR_BUILD_VERSION%.nupkg"
